import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  ratingBarStyle: {
    flexDirection: 'row',
  },
  starImageStyle: {
    width: 20,
    height: 20,
    resizeMode: 'cover',
    marginRight: 8,
  },
});

export default styles;
